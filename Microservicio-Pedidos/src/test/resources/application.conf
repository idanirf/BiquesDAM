ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ biques.dam.es.ApplicationKt.module ]
    }

    jwt {
        secret = "BiquesDAM"
        issuer = "BiquesUsuario"
        audience = "DAM"
        expiration = "3600"
    }
}

